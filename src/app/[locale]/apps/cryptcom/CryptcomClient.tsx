// src/app/[locale]/apps/cryptcom/CryptcomClient.tsx
'use client';

import React from "react";
import CryptcomPage from "./components/CryptcomPage";

const CryptcomClient = () => {
  return <CryptcomPage />;
};

export default CryptcomClient;